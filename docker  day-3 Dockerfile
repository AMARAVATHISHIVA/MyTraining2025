# 1. download the script
#
#   $ curl -fsSL https://get.docker.com -o install-docker.sh
#
# 2. verify the script's content
#
#   $ cat install-docker.sh
#
# 3. run the script with --dry-run to verify the steps it executes
#
#   $ sh install-docker.sh --dry-run
#
# 4. run the script either as root, or using sudo to perform the installation.
#
#   $ sudo sh install-docker.sh

-------------


docker run
 -it  interactive 
 -d    deattached 
 -p 8080:8080         imgae:vm
 jenkins/jenkins:latest
 
 
 docker run -it -d -p 8080:8080 jenkins/jenkins:latest
 
 
 466 MB 
 
 os  100 mb 
 Java   200 mb 
 Jenkins  100mb 
 
 --------------------
 
 cat /var/jenkins_home/secrets/initialAdminPassword  
 
 yes or no 
 
 
 how to go inside the Container 
 
 docker exec -it ca4816896cb7 bin/bash
 
 Set of Command's put in file is calld script 
 
 
 Windows 
 CMD    vs    RUN 
 
 
 
 -------
 
 Developers 
 
 apt-get update
 apt-get install nginx -y
 echo "Image created"
 
 --------
 
FROM ubuntu
MAINTAINER clouddevopshub@gmail.com
RUN apt-get update
RUN apt-get install nginx -y
CMD [“echo”,”Image created”]

-t tag
docker build -t mynginxbatch38 .

docker images  0

tage with your Docker account name 

docker tag <image-id of mynginxbatch17 > vikas4cloud/mynginxbatch21:latest


docker tag 7d88a448310e vikas4cloud/mynginxbatch38:latest

Docker login

Logein sucessfully 

docker push vikas4cloud/mynginxbatch38


-------

CW 
install docker 


sudo -i
apt-get update
apt install docker.io -y
docker pull vikas4cloud/mynginxbatch38

-

Lower OS size ? 

5mb Alpine -- OS --

YES in Production 

Alpine with java 


if u want to execute any Commnad on Container 
ENTRYPOINT 

Java 
ENV JAVA_HOME=C:\Program Files\Java\jdk-11\bin 

-p 8080:8080  Command line 
 
EXPOSE 8080      Docker file    -- K8s 

 
 
 -------
 
fresher 
 
FROM ubuntu
MAINTAINER clouddevopshub@gmail.com
RUN apt-get update
RUN apt-get install nginx -y
CMD [“echo”,”Image created”]
EXPOSE 80

Exp 3-5 Happy 
5 Exp  :- 

FROM alpine 
RUN apt-get update && apt-get install nginx -y
EXPOSE 80

Innovate Answer 

 


